import 'package:flutter/material.dart';

class Pagina3Pages extends StatelessWidget {
  const Pagina3Pages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red);
  }
}
