import 'package:flutter/material.dart';

class TermosDeUsoEPrivacidade extends StatelessWidget {
  const TermosDeUsoEPrivacidade({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Termos de Uso e Privacidade"),),

    );
  }
}